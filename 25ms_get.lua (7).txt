--[[ TAWIN ARMOR RUNTIME LOADER · FREE PUBLIC EDITION ]]--
local SCRIPT_UID = "ARMOR-CEMBZS"
local SERVER_URL = "https://armor.tawinhub.shop"
local _NONCE = "81f4df808132c27d"
local _SALT = "a636f6744bf6f256"
local _IS_FREE = true

local key = script_key or (getgenv and getgenv().script_key) or _G.script_key or (getgenv and getgenv().Key) or _G.Key or ""

if not _IS_FREE then
    if not key or key == "" or key == "PASTE_YOUR_KEY_HERE" then
        local alertMsg = "[TAWIN ARMOR ❌] กรุณาใส่ script_key ด้านบนก่อนรัน! (เช่น script_key = 'TAWIN-XXXX') สั่งซื้อได้ที่ tawinhub.shop"
        warn(alertMsg)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "TAWIN ARMOR ❌",
                Text = "กรุณาใส่ script_key ด้านบนก่อนรัน!",
                Duration = 10
            })
        end)
        return
    end
else
    key = "FREE"
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "TAWIN ARMOR 🎁",
            Text = "กำลังโหลดสคริปต์แจกฟรี (Free Edition)...",
            Duration = 5
        })
    end)
end

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local function get_hwid()
    local h = ""
    pcall(function()
        h = (gethwid and gethwid()) or (get_device_id and get_device_id()) or (identifyexecutor and identifyexecutor()) or tostring(LocalPlayer.UserId)
    end)
    return tostring(h)
end

local hwid = get_hwid()
local placeId = tostring(game.PlaceId)
local execName = "Unknown"
pcall(function()
    if identifyexecutor then execName = identifyexecutor() end
end)

local req_fn = (syn and syn.request) or (http and http.request) or http_request or request
if not req_fn then
    error("[TAWIN ARMOR] Your executor does not support HTTP requests!", 0)
    return
end

local ts = tostring(os.time())
-- Compute lightweight dynamic signature to defeat packet replay
local sig_raw = key .. ":" .. hwid .. ":" .. ts .. ":" .. _NONCE .. ":" .. _SALT
local sig_hash = 0
for i = 1, #sig_raw do
    sig_hash = (sig_hash * 31 + string.byte(sig_raw, i)) % 2147483647
end

local response = req_fn({
    Url = SERVER_URL .. "/api/stream.php",
    Method = "POST",
    Headers = { 
        ["Content-Type"] = "application/json",
        ["User-Agent"] = "Roblox/WinInet (" .. tostring(execName) .. ")"
    },
    Body = HttpService:JSONEncode({
        key = key,
        mode = _IS_FREE and "free" or "paid",
        hwid = hwid,
        place_id = placeId,
        script_uid = SCRIPT_UID,
        timestamp = ts,
        nonce = string.format("%08x%08x", math.random(1, 2147483647), math.random(1, 2147483647)),
        signature = string.format("%x", sig_hash),
        executor = execName
    })
})

if response and response.Body then
    local ok, data = pcall(function() return HttpService:JSONDecode(response.Body) end)
    if ok and data and data.success and data.payload then
        local exec, err = loadstring(data.payload)
        if exec then
            task.spawn(exec)
        else
            warn("[TAWIN ARMOR] Runtime Compilation Error:", err)
        end
    else
        local msg = (data and data.message) or "Access Denied / Verification Failed"
        warn("[TAWIN ARMOR ❌] " .. msg)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "TAWIN ARMOR",
                Text = msg,
                Duration = 10
            })
        end)
    end
else
    warn("[TAWIN ARMOR ❌] Could not reach security server.")
end
